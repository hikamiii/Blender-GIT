# Blender scene files are binary and benefit from Git LFS storage.  `lockable`
# prevents concurrent edits when collaborators use Git LFS locking.
*.blend filter=lfs diff=lfs merge=lfs -text lockable
